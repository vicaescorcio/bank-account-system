<p id="notice"><%= notice %></p>

<h4>Agencies</h4>

<table class="centered highlight responsive-table">
  <thead>
    <tr>
      <th>Ag</th>
      <th>Endereço</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @agencies.each do |agency| %>
      <tr>
        <td><%= agency.ag %></td>
        <td><%= agency.address %></td>
        <td><%= link_to 'Edit', edit_agency_path(agency) %></td>
        <td><%= link_to 'Destroy', agency, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<div id="agencies">
</div>
<%= link_to  new_agency_path,class:'btn-floating btn-large waves-effect waves-light red', remote: true do %>
<i class="material-icons">add</i>  
<%end%>